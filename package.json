{
  "name": "ajarbelajar-video-processor",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "start": "node src",
    "format": "prettier --write **/*.{js,json}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "winston": "^3.10.0"
  }
}
